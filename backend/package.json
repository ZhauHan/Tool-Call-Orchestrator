{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "cd .. && python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload",
    "test": "cd .. && python -m unittest discover -s backend/tests -t . -v",
    "cli": "python3 cli.py",
    "openai:smoke": "cd .. && python backend/scripts/openai_tool_visibility_smoke.py"
  }
}
